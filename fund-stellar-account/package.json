{
  "scripts": {
    "deploy": "wt create webtask.js -n fund-stellar-account -p colorglyph --secrets-file .secrets --bundle --bundle-minify"
  },
  "dependencies": {
    "auth0": "^2.9.1",
    "express": "^4.16.3",
    "stellar-sdk": "^0.8.0",
    "webtask-tools": "^3.2.1"
  }
}
